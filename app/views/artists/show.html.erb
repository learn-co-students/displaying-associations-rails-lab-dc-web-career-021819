<h1>Artist page for: <%= @artist.name %></h1>

<h2>Song count: <%= @artist.song_count %></h2>

<ul>
  <% @artist.songs.each do |s| %>
    <li><%= link_to s.title, song_path(s) %></li>
  <% end %>
</ul>
